= requesting_user.username
= link_to '参加許可', participants_path(user_id: requesting_user.id, meeting_id: meeting.id), class: 'btn btn-info btn-raised rounded', method: :post, remote: true
= link_to '参加拒否', requesting_path(requesting_user.requestings.find_by(meeting_id: meeting.id), user_id: requesting_user.id), class: 'btn btn-info btn-raised rounded', method: :delete, remote: true