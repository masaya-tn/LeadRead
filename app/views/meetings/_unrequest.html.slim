= link_to '申請取り消し', requesting_path(current_user.requestings.find_by(meeting_id: meeting.id), user_id: current_user.id), class: 'btn btn-info btn-raised rounded unrequest-button', method: :delete, remote: true
